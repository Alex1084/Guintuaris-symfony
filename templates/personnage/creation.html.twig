{% extends 'base.html.twig' %}

{% block title %}
	Création |
	{{parent()}}
{% endblock %}

{% block body %}
	<div class="container">
		{{ form_start(personnageForm) }}
		<div>
			<h3 class="title-form">Personnage</h3>
		</div>
		<div class="row">
			<div class="div-form">{{ form_label(personnageForm.nom) }}

				{{ form_widget(personnageForm.nom) }}
			</div>
			<div class="div-form">{{ form_label(personnageForm.niveau) }}

				{{ form_widget(personnageForm.niveau) }}
			</div>
			<div class="div-form">{{ form_label(personnageForm.race) }}

				{{ form_widget(personnageForm.race) }}
			</div>
			<div class="div-form">{{ form_label(personnageForm.classe) }}

				{{ form_widget(personnageForm.classe) }}
			</div>
		</div>
		<div>
			<h3 class="title-form">Statut</h3>
		</div>

		<div class="row">
			<div class="div-form">
				{{ form_label(personnageForm.pvMax) }}

				{{ form_widget(personnageForm.pvMax)}}
			</div>
			<div class="div-form">
				{{ form_label(personnageForm.pcMax) }}

				{{ form_widget(personnageForm.pcMax)}}
			</div>
			<div class="div-form">
				{{ form_label(personnageForm.pmMax) }}

				{{ form_widget(personnageForm.pmMax)}}
			</div>
		</div>

		<div>
			<h3 class="title-form">Statistiques</h3>
		</div>
		<div class="row">
			<div class="div-form">
				{{ form_label(personnageForm.constitution) }}

				{{ form_widget(personnageForm.constitution)}}
			</div>
			<div class="div-form">
				{{ form_label(personnageForm.laForce) }}

				{{ form_widget(personnageForm.laForce)}}
			</div>
			<div class="div-form">
				{{ form_label(personnageForm.dexterite) }}

				{{ form_widget(personnageForm.dexterite)}}
			</div>
		</div>
		<div class="row">
			<div class="div-form">
				{{ form_label(personnageForm.intelligence) }}

				{{ form_widget(personnageForm.intelligence)}}
			</div>
			<div class="div-form">
				{{ form_label(personnageForm.charisme) }}

				{{ form_widget(personnageForm.charisme)}}
			</div>
			<div class="div-form">
				{{ form_label(personnageForm.foi) }}

				{{ form_widget(personnageForm.foi)}}
			</div>
		</div>
		<div class="note">
			<p>Note : Les statistiques de niveaux se disposent de la façon suivante : 1x50, 1x40, 1x30, 3x20</p>
		</div>
		<div>
			<button class="valid-form">Créer</button>
		</div>

		{{ form_end(personnageForm) }}

	</div>
{% endblock %}
