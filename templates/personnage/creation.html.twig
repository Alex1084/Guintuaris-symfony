{% extends 'base.html.twig' %}

{% block title %}
	Création |
	{{parent()}}
{% endblock %}

{% block body %}
	<div class="container">
		{{ form_start(characterForm) }}
		<div>
			<h3 class="title-form">Personnage</h3>
		</div>
		<div class="row">
			<div class="div-form">{{ form_label(characterForm.name) }}

				{{ form_widget(characterForm.name) }}
			</div>
			<div class="div-form">{{ form_label(characterForm.level) }}

				{{ form_widget(characterForm.level) }}
			</div>
			<div class="div-form">{{ form_label(characterForm.race) }}

				{{ form_widget(characterForm.race) }}
			</div>
			<div class="div-form">{{ form_label(characterForm.class) }}

				{{ form_widget(characterForm.class) }}
			</div>
		</div>
		<div>
			<h3 class="title-form">Statut</h3>
		</div>

		<div class="row">
			<div class="div-form">
				{{ form_label(characterForm.pvMax) }}

				{{ form_widget(characterForm.pvMax)}}
			</div>
			<div class="div-form">
				{{ form_label(characterForm.pcMax) }}

				{{ form_widget(characterForm.pcMax)}}
			</div>
			<div class="div-form">
				{{ form_label(characterForm.pmMax) }}

				{{ form_widget(characterForm.pmMax)}}
			</div>
		</div>

		<div>
			<h3 class="title-form">Statistiques</h3>
		</div>
		<div class="row">
			<div class="div-form">
				{{ form_label(characterForm.constitution) }}

				{{ form_widget(characterForm.constitution)}}
			</div>
			<div class="div-form">
				{{ form_label(characterForm.strength) }}

				{{ form_widget(characterForm.strength)}}
			</div>
			<div class="div-form">
				{{ form_label(characterForm.dexterity) }}

				{{ form_widget(characterForm.dexterity)}}
			</div>
		</div>
		<div class="row">
			<div class="div-form">
				{{ form_label(characterForm.intelligence) }}

				{{ form_widget(characterForm.intelligence)}}
			</div>
			<div class="div-form">
				{{ form_label(characterForm.charisma) }}

				{{ form_widget(characterForm.charisma)}}
			</div>
			<div class="div-form">
				{{ form_label(characterForm.faith) }}

				{{ form_widget(characterForm.faith)}}
			</div>
		</div>
		<div class="note">
			<p>Note : Les statistiques de niveaux se disposent de la façon suivante : 1x50, 1x40, 1x30, 3x20</p>
		</div>
		<div>
			<button class="valid-form">Créer</button>
		</div>

		{{ form_end(characterForm) }}

	</div>
{% endblock %}
