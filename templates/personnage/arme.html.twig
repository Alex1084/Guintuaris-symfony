{% extends 'base.html.twig' %}

{% block title %}
	Mon équipement |
	{{parent()}}
{% endblock %}

{% block body %}
	{% if app.user.id == personnage.joueur.id %}
		<div class="container">
			{{ form_start(armureForm)}}

			<div>
				<h3 class="title-form">Armes</h3>
			</div>
			<div class="row">
				<div class="div-form">{{ form_label(armureForm.n_1) }}
					{{ form_widget(armureForm.n_1) }}

					{{ form_label(armureForm.effet_1) }}
					{{ form_widget(armureForm.effet_1) }}
				</div>

				<div class="div-form">{{ form_label(armureForm.n_2) }}
					{{ form_widget(armureForm.n_2) }}

					{{ form_label(armureForm.effet_2) }}
					{{ form_widget(armureForm.effet_2) }}
				</div>

				<div class="div-form">{{ form_label(armureForm.n_3) }}
					{{ form_widget(armureForm.n_3) }}

					{{ form_label(armureForm.effet_3) }}
					{{ form_widget(armureForm.effet_3) }}
				</div>
			</div>
			<button class="valid-form">Modifier !</button>
			{{ form_end(armureForm)}}
		</div>
	{% endif %}
{% endblock %}
