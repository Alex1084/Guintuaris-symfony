{% extends 'base.html.twig' %}
{% block title %} Ressources | {{parent()}}{% endblock %}

{% block body %}
	<h1 class="title">Ressources</h1>
	<div class="container">
		{% for message in app.flashes('success') %}
			<div class="flash success">
				{{ message }}
			</div>
		{% endfor %}
		<button class="default-sort">Tri par defaut</button>
		<table class="tableau">
			<thead>
				<tr>
					<th class="sortable number">Id</th>
					<th class="sortable text">Nom</th>
					<th class="sortable text">Symbole</th>
					<th></th>
				</tr>
			</thead>
			<tbody>
				{% for resource in resources %}

					<tr>
						<td> {{resource.id}} </td>
						<td> {{ resource.label }} </td>
						<td> {{ resource.symbol }} </td>
						<td>
							<a href="{{ path("admin_update_resource", {'id' : resource.id}) }}">Modifier</a>
						</td>
					</tr>
						
				{% endfor %}
			</tbody>
		</table>
	</div>
{% endblock %}

{% block navside %}
	{% set navsideDisplay = true %}
	{% set navsideLinks = [
		jsonLinks.returnAdmin,
		jsonLinks.createResource,
		jsonLinks.duration,
		jsonLinks.statistic,
		jsonLinks.skills,
	]%}
	{{parent()}}
{% endblock %}

{% block script %}
    <script src="{{asset('script/tableau.js')}}"></script>
	<script src="{{ asset("script/delete.js")}}"></script>
{% endblock %}