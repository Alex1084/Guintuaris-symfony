{% extends 'base.html.twig' %}
{% block title %}
	Les équipes |
	{{parent()}}
{% endblock %}

{% block body %}
	<h2>Fiches personnages</h2>
	<div class="container">
    <div class="">
        <a href="{{ path("admin_add_class")}}">Creer une Nouvelle Competence</a>
    </div>
		<table class="tableau">
			<tr>
				<th>Id</th>
				<th>Name</th>
                <th></th>
			</tr>
			{% for class in classs %}

				<tr>
					<th> {{class.id}} </th>
					<th> {{ class.name }} </th>
                    <th>
                        <a href="{{ path("admin_update_class", {'classId' : class.id}) }}">Modifier</a>
                    </th>
				</tr>
					
			{% endfor %}
		</table>
	</div>
{% endblock %}


{% block script %}
	<script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
	<script src="{{ asset("script/delete.js")}}"></script>
{% endblock %}