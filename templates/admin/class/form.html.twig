{% extends 'base.html.twig' %}

{% block title %}{{(requestAttribute == "admin_update_class") ? "Modifier une classe":"Ajouter une classe"}} | {{parent()}}
{% endblock %}

{% block body %}
	<div class="container">
		{{ form_start(classForm) }}
			<div>
				<h3 class="title-form">Géneral</h3>
				<div>
					<div class="div-form">{{ form_label(classForm.name) }}
						
						{{ form_widget(classForm.name) }}
						<div class="div-errors">
							{{form_errors(classForm.name)}}
						</div>
					</div>
				</div>
			</div>

		<div class="row">	
			<div class="div-form">{{ form_label(classForm.dicePv) }}
				
				{{ form_widget(classForm.dicePv) }}
				<div class="div-errors">
					{{form_errors(classForm.dicePv)}}
				</div>
			</div>

			<div class="div-form">{{ form_label(classForm.dicePm) }}
				
				{{ form_widget(classForm.dicePm) }}
				<div class="div-errors">
					{{form_errors(classForm.dicePm)}}
				</div>
			</div>
			<div class="div-form">{{ form_label(classForm.dicePc) }}
				
				{{ form_widget(classForm.dicePc) }}
				<div class="div-errors">
					{{form_errors(classForm.dicePc)}}
				</div>
			</div>
		</div>

			<div class="row">
				<div class="textarea-form">
					<h3 class="title-form">
						{{ form_label(classForm.description) }}
					</h3>
					
					{{ form_widget(classForm.description) }}
					<div class="div-errors">
						{{form_errors(classForm.description)}}
					</div>

				</div>
			</div>

		<button class="valid-form">Valider !</button>
		{{form_end(classForm)}}
	</div>
{% endblock %}
