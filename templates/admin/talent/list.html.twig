{% extends 'base.html.twig' %}
{% block title %} Statistiques | {{parent()}}{% endblock %}

{% block body %}
	<h1 class="title">Statistiques</h1>
	<div class="container">
		{% for message in app.flashes('success') %}
			<div class="flash success">
				{{ message }}
			</div>
		{% endfor %}
		<button class="default-sort">Tri par defaut</button>
		<table class="tableau">
			<thead>
				<tr>
					<th class="sortable number">Id</th>
					<th class="sortable text">Nom</th>
					<th class="sortable text">Statistiques</th>
					<th></th>
				</tr>
			</thead>
			<tbody>
				{% for talent in talents %}

					<tr>
						<td> {{talent.id}} </td>
						<td> {{ talent.name }} </td>
						<td> {{ talent.statistic.name }} </td>
						<td>
							<a href="{{ path("admin_update_talent", {'id' : talent.id}) }}">Modifier</a>
						</td>
					</tr>
						
				{% endfor %}
			</tbody>
		</table>
	</div>
{% endblock %}

{% block navside %}
	{% set navsideDisplay = true %}
	{% set navsideLinks = [
		jsonLinks.returnAdmin,
		jsonLinks.createTalent,
		jsonLinks.duration,
		jsonLinks.resource,
		jsonLinks.skills,
	]%}
	{{parent()}}
{% endblock %}
