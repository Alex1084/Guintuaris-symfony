{% extends 'base.html.twig' %}
{% block title %}
	Les équipes |
	{{parent()}}
{% endblock %}

{% block body %}
	<h2>Fiches personnages</h2>
	<ul>
		{% for element in list %}

			<li>
				{{ element.nom }}
			</li>
		{% endfor %}
	</ul>

	{{form_start(form) }}
	{{form_widget(form) }}
	<button>Créer</button>
	{{form_end(form) }}
{% endblock %}
