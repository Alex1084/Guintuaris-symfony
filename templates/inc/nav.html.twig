<header>
    <div id="div-logo">
        <img src="{{asset('img/logo.png')}}" alt="logo_guituaris">
    </div>
    <div class="nav-div">
        <nav class="nav-list">
            <div class="nav-item"><a title="Accueil" class="link" href="{{ path('main_home') }}">Accueil</a></div>
            <div class="drop-nav">
                <div class="nav-item"><button class="nav-button">Races</button></div>
                <div class="drop-nav-content">
                    <ul id="race-list">
                    </ul>
                </div>
            </div>
            <div class="drop-nav">
                <div class="nav-item"><button class="nav-button">Classes</button></div>
                <div class="drop-nav-content">
                    <ul id="class-list">
                    </ul>
                </div>
            </div>
            {% if app.user %}
                <div class="drop-nav">
                    <div class="nav-item"><button class="nav-button">Regle</button></div>
                    <div class="drop-nav-content">
                        <ul>
                            <li class="nav-item"><a href="{{ path("teaser_rules") }}">Repos</a></li>
                            <li class="nav-item"><a href="{{ path("teaser_rules") }}">Statistique</a></li>
                            <li class="nav-item"><a href="{{ path("teaser_rules") }}">Talent</a></li>
                            <li class="nav-item"><a href="{{ path("teaser_rules") }}">Niveaux</a></li>
                            <li class="nav-item"><a href="{{ path("teaser_rules") }}">Deplacement</a></li>
                        </ul>
                    </div>
                </div>
                <div class="nav-item"><a title="Tableau de metre du jeu" class="link" href="{{path('teaser_office')}}">Le bureau</a></div>
                <div class="nav-item"><a title="Tableau de metre du jeu" class="link" href="https://www.tiki-toki.com/timeline/entry/1581307/frise-guintuaris" target="_blank">Frise</a></div>
            {% endif %}

            <div id="account">
                {% if app.user %}
                    <div class="drop-nav">
                        <img src="{{asset("/assets/icons/scroll-quill.svg")}}" width="40" alt="">
                        <div class="drop-nav-content">
                            <ul>
                                {% if is_granted("ROLE_MASTER") %}
                                    <li class="nav-item"><a title="Liste des pesonnages et des equipes" href="{{path('master_list_character_team')}}">Mes personnages et mes Equipes</a></li>
                                    <li class="nav-item"><a title="Tableau de metre du jeu" href="{{path('board')}}">Mon Tableau de maitre du jeu</a></li>
                                {% elseif is_granted("ROLE_USER") %}
                                    <li class="nav-item"><a title="Liste des personnages" href="{{path('character_list')}}">Mes personnages</a></li>
                                {% endif %}
                                {% if is_granted("ROLE_ADMIN") %}
                                    <li class="nav-item"><a title="Administration" href="{{path('admin_home')}}">Administration</a></li>
                                {% endif %}
                            </ul>
                        </div>
                    </div>
                {% endif %}
                <div class="drop-nav">
                    {% if app.user and is_granted("ROLE_USER") %}
                        <img src="{{asset("/assets/icons/brutal-helm.svg")}}" width="40" alt="">
                        <div class="drop-nav-content">
                            <ul>
                                <li class="nav-item"><a title="Voir mon Compte" href="{{ path('profil') }}">Mon compte ({{ app.user.name }})</a></li>
                                <li class="nav-item"><a title="Déconnexion" href="{{ path('app_logout')}}">Déconexion</a></li>
                            </ul>
                        </div>
                    {% else %}
                        <img src="{{asset("/assets/icons/brutal-helm.svg")}}" width="40" alt="">
                        <div class="drop-nav-content">
                            <ul>
                                <li class="nav-item"><a title="Connexion" href="{{ path('app_login')}}">Connexion</a></li>
                                <li class="nav-item"><a title="Inscription" href="{{ path('app_register')}}">S'inscrire</a></li>
                            </ul>
                        </div>
                    {% endif %}
                </div>
            </div>
        </nav>
    </div>
</header>